# frozen_string_literal: true
module PassengerMobileUI
  def self.start(user_id)
    puts "Mobile Passenger UI started for #{user_id}"
    # placeholder: in real app this would mount a mobile view
  end
end
